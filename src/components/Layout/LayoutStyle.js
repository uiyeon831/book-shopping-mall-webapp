import Styled from "styled-components";

const DivMain = Styled.div`
  display: flex;
  flex-direction: column;
`;

const CenterDiv = Styled.div`
  display: flex;
  flex-direction: row;
`;

export { DivMain, CenterDiv };