import styled from "styled-components";

const Logo = styled.p`
  width: 70px;
  font-size: 28px;
  font-weight: 600;
  text-align: left;
  &:hover {
    cursor: pointer;
  }
`

const ATag = styled.a`
  text-decoration: none;
`

export {Logo, ATag};